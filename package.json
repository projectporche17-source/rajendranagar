{
  "name": "rajendranagar.online",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:worker": "wrangler deploy --env production worker/worker.js",
    "deploy:secrets": "wrangler secret put TURSO_URL --env production && wrangler secret put TURSO_TOKEN --env production",
    "deploy:full": "npm run deploy:secrets && npm run deploy:worker"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "@libsql/client": "^0.15.15"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
